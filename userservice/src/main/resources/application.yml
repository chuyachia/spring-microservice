server:
  port: 9001

spring:
  application:
    name: USER-SERVICE

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost

jwt:
  secret: E(H+MbQeThWmZq4t7w!z%C*F-J@NcRfU
  validity-millis: 600000